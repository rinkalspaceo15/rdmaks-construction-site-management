# Local agent/editor state — not shared
.claude/

# Dependencies & build output (also ignored per-package)
node_modules/
dist/
build/

# Environment & secrets
.env
.env.*
!.env.example

# Logs & OS files
*.log
.DS_Store
